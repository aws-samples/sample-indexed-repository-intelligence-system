# Development environment - no auth.
# Local dev talks to a locally-run AgentCore Runtime entrypoint on :8080.
VITE_AUTH_ENABLED=false
VITE_APP_NAME="IRIS (Dev Local)"
VITE_LOCAL_AGENT_URL=http://localhost:8080
